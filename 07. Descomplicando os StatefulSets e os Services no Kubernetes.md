<h2 align="center"> Descomplicando os StatefulSets e os Services no Kubernetes. </h2>

<h3 align="center"> 07.1.	O que é um StatefulSet? </h3>

Os StatefulSets são uma funcionalidade do Kubernetes que gerencia o deployment e o scaling de um conjunto de Pods, fornecendo garantias sobre a ordem de deployment e a singularidade desses Pods.

Diferente dos Deployments e Replicasets que são considerados stateless (sem estado), os StatefulSets são utilizados quando você precisa de mais garantias sobre o deployment e scaling. Eles garantem que os nomes e endereços dos Pods sejam consistentes e estáveis ao longo do tempo.

**O que é um StatefulSet no Kubernetes?**  
StatefulSet é um tipo de controlador usado para rodar aplicações stateful (com estado persistente), onde:

* Cada pod tem identidade fixa;
* Cada pod tem seu próprio volume persistente;
* A ordem de criação e remoção importa;
* Nomes e DNS são previsíveis.

Diferente de Deployments, onde os Pods são descartáveis e idênticos.  

<h3 align="center"> 07.2. Criando o nosso primeiro StatefulSet. </h3>

Nessa aula, crie o arquivo **07_statefulset-nginx.yml** para criar o nosso primeiro StatefulSet.  
<p align="center"><img width="329" height="476" alt="image" src="https://github.com/user-attachments/assets/53f38f2b-44d3-4093-b7f2-16e45d95e674" /></p>

Abaixo, todas as configurações explicadas:  

* **apiVersion**: indica a versão da API do Kubernetes usada para criar o recurso. A versão **v1** é a API estável para criar recursos básicos (Pod, Service, ConfigMap).  
* **kind**: define o tipo de recurso que será criado, nesse caso, StatefulSet.  
* **metadata**: informações relacionadas ao StatefulSet.  
* **spec**: nome do Headless service associado.  
  * **serviceName**: nome do serviço.  
  * **replicas**: número de réplicas dos Pods.  
  * **selector**: tudo dentro do bloco selector define quais pods pertencem a esse StatefulSet.  

* **template**: esse bloco define como cada pod será criado. Os labels desse bloco precisam casar com o **selector**.  
* **spec**: define os containers do Pod. O **name** indica o nome interno do container.  
* **ports** e **containerPorts**: definem a porta que o container irá usar.  

* **volumeMounts**: monta o volume persistente no container.  
  * **name**: nome do volume persistente.  
  * **mountPath**: diretório que será montado no container.  

* **volumeClaimTemplates**: define um template de PersistentVolumeClaim. O Kubernetes cria um PVC por pod.
  * **metadata**: indica o name do PVC.

* **spec**: indica as especificações do PVC.  
  * **accessModes**: modo de acesso ao volume. **ReadWriteOnce** indica que o volume pode ser montado por um nó por vez.  
* **resources**:  
  * **requests**:  
  * **storage**: solicita 1Gib de armazenamento.  

**Para aplicar a configuração, execute**: kubectl apply -f 07_statefulset-nginx.yml  
**Para visualizar os StatefulSets existentes**: kubectl get sts  
**Para ver os StatefulSets com mais detalhes**: kubectl get sts **-o wide**  
<p align="center"><img width="421" height="39" alt="image" src="https://github.com/user-attachments/assets/cba9982a-2087-478c-b9cd-750550ee8702" /></p>

Verifique os Pods criados por esse StatefulSet:  
<p align="center"><img width="359" height="68" alt="image" src="https://github.com/user-attachments/assets/22765104-2bab-4122-bb5d-d488a7bd2e72" /></p>

Execute **kubectl get pvc** para ver os PersistentVolumeClaims criados pelo StatetfulSet:  
<p align="center"><img width="569" height="63" alt="image" src="https://github.com/user-attachments/assets/8e391694-7d5f-4af6-8f21-27428bdd8a31" /></p>

Execute também **kubectl get pv** para ver os PersistentVolumes criados:  
<p align="center"><img width="794" height="65" alt="image" src="https://github.com/user-attachments/assets/c3901c25-b2f4-403a-8e63-4d0e3b76cf60" /></p>

<h3 align="center"> 07.3.  Criando o nosso Headless Service. </h3>

**O que é Headless Service?**  
Primeiramente, Headless Service no Kubernetes é um Service do Kubernetes sem IP virtual (ClusterIP).  

Ou seja, ele não faz Load Balancer e não esconde os Pods atrás de um único IP, em vez disso, ele expõe diretamente os IPs dos Pods via DNS.  

Para criar um Headless Service, crie o arquivo **07_headless-svc.yml** conforme o exemplo abaixo:  
<p align="center"><img width="195" height="219" alt="image" src="https://github.com/user-attachments/assets/56583943-bfbc-48f7-b235-a413af759c12" /></p>

* **Para aplicar a configuração, execute**: kubectl apply -f 07_headless-svc.yml  
* **Para ver o Headless Service criado, execute**: k get svc  
* **Para ver o Headless Service existentes, com mais detalhes**: k get svc **-o wide**  
<p align="center"><img width="571" height="52" alt="image" src="https://github.com/user-attachments/assets/8a6330b4-adb9-4627-a308-e5bdc14a743f" /></p>

**Exemplo de configuração do NGINX.**  
Acesse o Pod **nginx-0** com o comando **kubectl exec -ti nginx-0 -- bash**  
* **Crie um arquivo em /usr/share/nginx/html**: echo "GIROPOPS NGINX-O" > index.html  
* Acesse o pod **nginx-1** e execute **curl nginx-0.nginx.default.svc.cluster.local**
<p align="center"><img width="493" height="32" alt="image" src="https://github.com/user-attachments/assets/e0268aad-e334-4882-a76d-76453f30e0ac" /></p>

Como outro teste, execute o comando abaixo nos três Pods, alterando apenas o número do Pod:  
* echo "GIROPOPS NGINX-**1**" >> /usr/share/nginx/html/index.html  

Após executar um curl para todos os Pods:  
<p align="center"><img width="493" height="100" alt="image" src="https://github.com/user-attachments/assets/81cb4d10-72c2-437f-831b-b390c25fc2b1" /></p>

Mesmo após remover algum dos Pods informados acima, a configuração de **replicas** cria o Pod novamente e a configuração de PV armazena o arquivo criado em /usr/share/nginx/html/index.html

<h3 align="center"> 07.4. Removendo o nosso StatefulSet. </h3>

Realize a limpeza do ambiente executando os comandos abaixo:  
* kubectl delete sts nginx  
* kubectl delete svc nginx  
* kubectl delete pv  
* kubectl delete pvc  

<h3 align="center"> 07.5. O que são os Services? </h3>

Os Services no Kubernetes são uma abstração que define um conjunto lógico de Pods e uma política para acessá-los. Eles permitem que você exponha uma ou mais Pods para serem acessados por outros Pods, independentemente de onde eles estejam em execução no cluster.

Os Services são definidos usando a API do Kubernetes, normalmente através de um arquivo de manifesto YAML.

Existem quatro tipos principais de Services:
* **ClusterIP**: é o padrão. Expõe o Service em um IP interno no cluster. Este tipo torna o Service acessível apenas dentro do Cluster.  
* **NodePort**: expõe o Service na mesma porta de cada Node selecionado no cluster usando NAT. Torna o Service acessível de fora do Cluster.  
* **LoadBalancer**: Cria um balanceador de carga externo no ambiente de nuvem atual (se suportado) e atribui um IP fixo, externo ao cluster, ao Service.  
* **ExternalName**: mapeia o Service para o conteúdo do campo externalName (por exemplo, foo.bar.example.com), retornando um registro CNAME com seu valor.  

**Como os Services funcionam?**  
Os Services no Kubernetes fornecem uma abstração que define um conjunto lógico de Pods e uma política para acessá-los. Os conjuntos de Pods são determinados por meio de seletores de rótulo (Label Selectors). Embora cada Pod tenha um endereço IP único, esses Ips não são expostos fora do cluster sem um serviço.

Sempre é bom reforçar a importância dos Labels no Kubernetes, pois eles são a base para a maioria das operações no Kubernetes, então cuide com carinho dos seus Labels.  

<h3 align="center"> 07.6. Criando os nossos Services ClusterIP e NodePort. </h3>

Para criar um exemplo básico para expor um serviço com ClusterIP, execute os passos abaixo:  
* **Crie um deployment**: kubectl create deployment nginx --image nginx --port 80  
* **Expor o serviço**: kubectl expose deployment nginx --type NodePort  
* **Para ver mais detalhes desse serviço**: kubectl describe svc nginx  
<p align="center"><img width="468" height="289" alt="image" src="https://github.com/user-attachments/assets/a6cfaf8d-859e-400f-a226-b4b3e02da806" /></p>

**kubectl expose**  
O comando **kubectl expose** é um atalho do Kubernetes para criar um **Service** rapidamente a partir de um recurso que já existe (pod, service, replicationcontroller, deployment ou replicaset).  

**O que é Endpoints em um Service no Kubernetes?**  
Endpoints são a lista real de IPs e portas dos Pods que um Service vai encaminhar o tráfego.  

**Como isso funciona na prática?**  
* Você cria um Pod / Deployment.  
* Os Pods recebem os labels.  
* Você cria um Service com **selector**.  
* O Kubernetes cria automaticamente um objeto **Endpoints**.  
* Esse objeto contém os IPs dos Pods que casam com o **selector**.  

**Para ver os endpoints existentes, execute:** kubectl get endpoints  



