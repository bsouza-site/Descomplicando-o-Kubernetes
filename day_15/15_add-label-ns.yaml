apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: add-label-ns
spec:
  rules:
  - name: add-label-ns
    match:
      resources:
        kinds:
        - Namespace
    mutate:
      patchStrategicMerge:
        metadata:
          labels:
            NS: "teste_Kyverno"
