<h2 align="center"> Descomplicando ReplicaSets, DaemonSets e as Probes do Kubernetes. </h2>

<h3 align="center"> 04.1.	O que é um ReplicaSet? </h3>

O objetivo de um ReplicaSet é manter um conjunto estável de Pods de réplica em execução a qualquer momento. Como tal, ele é frequentemente usado para garantir a disponibilidade de um número específico de Pods idênticos.

Sempre que você cria um Deployment, o Kubernetes cria automaticamente um ReplicaSet para ele, isso é comportamento padrão do Kubernetes. Após isso, é o ReplicaSet que cria os Pods que estão dentro do Deployment.

Outro ponto importante, quando estamos fazendo a atualização de uma versão de um Pod com o Deployment, o Deployment cria um novo ReplicaSet para fazer o gerenciamento das réplicas dos Pods e quando a atualização termina, o Deployment remove as réplicas do ReplicaSet antigo e deixa apenas as réplicas do ReplicaSet novo.

Mas ele não remove o ReplicaSet antigo, ele o deixa lá, pois ele pode ser usado para fazer um Rollback da versão do Pod caso algo dê errado. Sim, quando precisamos fazer o Rollback de uma atualização em nossos Pods, o Deployment somente muda o ReplicaSet que está sendo usado para fazer o gerenciamento das réplicas dos Pods, passando a utilizar o ReplicaSet antigo.

**Quando usar um ReplicaSet?**  
Um ReplicaSet garante que um número específico de réplicas de pods esteja em execução a qualquer momento. No entanto, um Deployment é um conceito de nível superior que gerencia ReplicaSets e fornece atualizações declarativas para Pods, além de muitos outros recursos úteis. Portanto, o Kubernetes recomenda o uso de Deployments em vez de ReplicaSets diretamente, a menos que você precise de orquestração de atualizações personalizada ou não precise de atualizações.

Isso significa que você pode nunca precisar manipular objetos ReplicaSet: use um Deployment em vez disso e defina sua aplicação na seção de especificação.  

<h3 align="center"> 04.2. O Deployment e o ReplicaSet. </h3>

* **Para ver os ReplicaSets criados, execute**: `kubectl get rs`  
* **Para ver os ReplicaSets criados com mais detalhes**: `kubectl get rs -o wide`
<p align="center"><img width="514" height="116" alt="image" src="https://github.com/user-attachments/assets/968e5b1c-9af7-49a6-8efe-80f92e826082" /></p>

Para entender melhor a saída desse comando:  
* **nginx-deployment**: é o nome do deployment.  
* **5559c8d6c4**: é o hash do ReplicaSet.  

**Para ver detalhes do ReplicaSet**: `kubectl describe rs`  

Abaixo, a saída dos comandos **get deployment**, **get rs** e **get pods** mostrando a conexão entre esses itens.
<p align="center"><img width="568" height="258" alt="image" src="https://github.com/user-attachments/assets/7e779f3f-4241-4218-b103-cc7f6dc0d4d3" /></p>

<h3 align="center"> 04.3. Criando o nosso ReplicaSet. </h3>

Para criação do nosso primeiro ReplicaSet, crie o arquivo **04_nginx-replicaset.yml** conforme o conteúdo abaixo:  
<p align="center"><img width="219" height="419" alt="image" src="https://github.com/user-attachments/assets/87f76955-7c60-4dfb-a55e-a6921ea54063" /></p>

O único parâmetro diferente de um Deployment é o **kind: ReplicaSet**.

* **Para aplicar a configuração criada**: `kubectl apply -f 04_nginx-replicaset.yml`  
* **Para ver os ReplicaSets criados**: `kubectl get rs`  
* **Para ver informações dos ReplicaSets existentes**: `kubectl describe rs`  
* Inclua **-o wide** para ver as informações com mais detalhes.  
* **Para remover um ReplicaSet**: `kubectl delete rs nginx-replicaset`  
* **Remover usando o arquivo de configuração**: `kubectl delete -f 04_nginx-replicaset.yml`  

<h3 align="center"> 04.4.	O que é um DaemonSet. </h3>

O **DaemonSet** é um objeto que garante que todos os nós do cluster executem uma réplica de um Pod, ou seja, ele garante que todos os nós do cluster executem uma cópia de um Pod.

O DaemonSet é muito útil para executar Pods que precisam ser executados em todos os nós do cluster, como por exemplo, um Pod que faz o monitoramento de logs, ou um Pod que faz o monitoramento de métricas.

Alguns casos de uso de DaemonSets são:  
* Execução de agentes de monitoramento, como o Prometheus Node Exporter ou o Fluentd;
* Execução de um proxy de rede em todos os nós do cluster, como kube-proxy, Weave Net, Calico ou Flannel.
* Execução de agentes de segurança em cada nó do cluster, como Falco ou Sysdig.

Portanto, se nosso cluster possuir 3 nós, o DaemonSet vai garantir que todos os nós executem uma réplica do Pod que ele está gerenciando, ou seja, 3 réplicas do Pod.

Caso adicionemos mais um node ao cluster, o DaemonSet vai garantir que todos os nós executem uma réplica do Pod que ele está gerenciando, ou seja, 4 réplicas do Pod.

<h3 align="center"> 04.5.  Criando o nosso DaemonSet. </h3>

Neste momento, irei criar um DaemonSet em um arquivo manifesto. Para isso, crie o arquivo **04_daemonset.yml** conforme exemplo abaixo:
<p align="center"><img width="333" height="582" alt="image" src="https://github.com/user-attachments/assets/06cbf04e-6445-4b66-a85c-48dc0c5f7489" /></p>

* **Para aplicar esse daemonSet**: `kubectl apply -f daemonset.yml`  
* **Para ver os daemonSets criados**: `kubectl get daemonsets (ds)`  
* **Para ver detalhes do daemonSet**: `kubectl describe ds node-exporter-daemonset`  
* **Para deletar o daemonSet**: `kubectl delete ds node-exporter-daemonset`  

Abaixo, a implantação dos daemonSets com um pod em cada node.  
<p align="center"><img width="570" height="25" alt="image" src="https://github.com/user-attachments/assets/f72cb2f6-7696-439f-b559-cd2065ee0415" /></p>

Apesar das configurações acima serem muito parecidas com o Deployment e ReplicaSet, devemos entender cada campo:

* **apiVersion**: indica a versão da API do Kubernetes usada para criar o recurso. A versão **v1** é a API estável para criar recursos básicos (Pod, Service, ConfigMap).  
* **kind**: define o tipo de recurso que será criado, nesse caso, **DaemonSet**.  
* **metadata**: informações relacionadas ao Pod.  
  * **labels**: labels que serão adicionadas aos Pods.  
* **name**: o name desse DaemonSet.  
* **spec**: especificações do DaemonSet.  
  * **selector**: define quais pods pertencem a esse DaemonSet.  
  * **matchLabels**: deve usar o mesmo labels informado em metadata.  
* **template**: template do objeto, nesse caso, Pod.  
  * **metadata**: informações sobre esse objeto.  
  * **labels**: labels que serão adicionadas aos Pods.  
  * **app**: label que será adicionada aos Pods.  
* **spec**: especificações do Pod.  
* **hostNetwork**: true indica que o POD irá usar o IP do node e expões as portas diretamente no host.  
* **containers**: lista de containeres que serão executados no Pod.  
  * **name**: nome do container.  
  * **image**: imagem do container.  
  * **ports**: lista de portas que serão expostas no container.  
  * **containerPort**: porta que será exposta no container.  
  * **hostPort**: porta que será exposta no host.  
  * **volumeMounts**: lista de volumes que serão montados no container.  
  * **name**: nome do volume.  
  * **mountPath**: caminho onde o volume será montado no container.  
  * **readOnly**: habilita o modo de apenas leitura.  
* **volumes**: lista de volumes que serão utilizados no Pod.  
  * **name**: nome do volume.  
  * **hostPath**: tipo de volume.  
  * **path**: caminho do volume no host.  


