apiVersion: v1
kind: Pod
metadata:
  name: meu-pod-leitor
  namespace: default
spec:
  serviceAccountName: pod-leitor
  containers:
  - name: pod-leitor-1
    image: alpine
    command: ["sleep", "infinity"]
