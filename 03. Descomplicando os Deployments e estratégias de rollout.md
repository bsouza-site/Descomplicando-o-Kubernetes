<h2 align="center"> Descomplicando os Deployments e estratégias de rollout. </h2>

<h3 align="center"> 03.1. O que é um Deployment?</h3>

O **Deployment** no Kubernetes é um recurso que define o estado desejado de uma aplicação em execução. Ele permite que você descreva, atualize e gerencie aplicações de forma declarativa. Ao utilizar Deployments, você não precisa se preocupar com os detalhes da implementação, pois o Kubernetes cuidará automaticamente do processo de implantação e atualização.

Um Deployment permite que você:

* Defina quantas réplicas da aplicação deve existir;
* Garanta alta disponibilidade;
* Realize atualizações sem downtime (rolling update);
* Faça rollback para versões anteriores;
* Mantenha os pods sempre no estado desejado.

Em resumo: você descreve **o estado desejado**, e o Kubernetes garante que ele seja mantido.  

<h3 align="center"> 03.2. Criando um Deployment através de um manifesto. </h3>

Criar um Deployment via manifesto no Kubernetes significa definir a aplicação em um arquivo YAML e pedir para o Kubernetes criar e gerenciar esse Deployment com base nessa definição.

Para iniciar a criação de um arquivo de manifesto, crie o arquivo **03_deployment.yaml** com o conteúdo abaixo:
<p align="center"><img width="253" height="441" alt="image" src="https://github.com/user-attachments/assets/dfcece3e-d3b4-42b7-bc36-301ebae24d7a" /></p>

* **Para aplicar este deployment, execute**: kubecl apply -f deployment.yaml
* **Para ver o deployment criado**: kubectl get deployments -o wide
* **Para ver mais detalhes desse deployment**: kubectl describe deployments
* **Ver os pods desse deployment pela label**: k get pods -l app=nginx-deployment
* **Para remover um deployment, execute**: kubectl delete -f deployment.yaml

Nas linhas abaixo, vamos entender o que significa cada parâmetro:

* **apiVersion**: indica a versão da API do Kubernetes usada para criar o recurso. A versão v1 é a API estável para criar recursos básicos (Pod, Service, ConfigMap).  
* **kind**: define o tipo de recurso que será criado, nesse caso, **Deployment**.  
* **metadata**: informações relacionadas ao **Deployment**.  
  * **labels**: rótulos usados para organização e seleção.  
  * **name**: nome do Deployment. Deve ser único dentro do namespace.  
* **spec**: especificações de como o Deployment deve se comportar.  
* **replicas**: quantidade de réplicas dos Pods.  
* **selector**: define quais pods pertencem a esse deployment.  
  * **matchLabels**: este campo deve combinar exatamente com os labels dos Pods. Isso evita que o deployment gerencie pods errados.  
* **strategy**: define a estratégia de atualização (rolling update, recreate etc.). Como está vazio, o Kubernetes usa o padrão rolling update, isso atualiza os pods aos poucos, sem downtime.  
* **template**: define o modelo dos Pods que o Deployment cria.  
  * **metadata**: informações relacionadas aos Pods que serão criados.  
  * **labels**: os labels que serão aplicados aos Pods. Precisam ser os mesmos do matchLabels.  
* **spec**: essa segunda especificação, são as especificações dos containeres.

Após essa implantação, os pods foram criados corretamente.
<p align="center"><img width="552" height="83" alt="image" src="https://github.com/user-attachments/assets/3d5bc74c-f802-4dfc-92d4-c74bf5cb5c85" /></p>

Sempre que um deployment é criado, é criado também um replicaset... para ver o replicaset criado, execute: **kubectl get replicasets**  

<h3 align="center"> 03.3. Criando mais Deployments e vendo os detalhes. </h3>

Para ver mais detalhes desse deployment criado, execute **kubectl describe deployments**
<p align="center"><img width="722" height="609" alt="image" src="https://github.com/user-attachments/assets/7d2a3608-ae0d-450f-9c17-b205b175d155" /></p>

É possível visualizar a saída do comando **kubectl get deployment** no formato YAML com a opção **-o yaml**  

Abaixo, outras opções para trabalhar com deployments:  
* **Para criar um deployment via linha de comando, execute**: kubectl create deployment --image **nginx** --replicas **3 nginx-deployment**  
* **Também é possível usar o dry-run para simular a criação de um deployment**: kubectl create deployment --image **nginx** --replicas **3 nginx-deployment** --dry-run=client  
* **Para gerar um arquivo de deployment, use o dry-run e direcione a saída do comando para um arquivo**: kubectl create deployment --image **nginx** --replicas **3 nginx-deployment** --dry-run=client -o yaml > **new-dep.yaml**  

<h3 align="center"> 03.4. Como atualizar um Deployment? </h3>

No arquivo de Deployment anterior, atualize os seguintes campos:  

* Em **metadata**, inclua um novo namespace **namespace: giropops**  
* Na imagem de container, mude a imagem para **nginx:1.15.0**  

**Crie o novo namespace**: kubectl create namespace giropops  
**Aplique o Deployment atualizado**: k apply -f 03_deployment.yaml

<h3 align="center"> 03.5. Estratégias de atualizações de nossos Deployments. </h3>

Neste momento, vamos entender como funcionam as estratégias de atualizações dos Deployments.  

**RollingUpdateStrategy**  
O **RollingUpdateStrategy** conforme exemplo abaixo é a estratégia padrão de atualização de um Deployment. Ela define como os Pods antigos são substituídos pelos novos, sem derrubar tudo de uma vez.
<p align="center"><img width="479" height="21" alt="image" src="https://github.com/user-attachments/assets/f656986a-79d3-426a-b811-536803ac52e7" /></p>

De acordo com a imagem acima, durante uma atualização:

* No máximo 25% dos Pods podem ficar indisponíveis;  
* No máximo 25% de Pods extras podem ser criados.

Abaixo, a configuração de uma estratégia **RollingUpdate**:
<p align="center"><img width="200" height="87" alt="image" src="https://github.com/user-attachments/assets/111663b1-8369-421e-a3cc-90062d3fd599" /></p>

Para entender cada característica dessa configuração:

* **type**: tipo de estratégia de deployment, nesse caso, **RollingUpdate**.  
* **rollingUpdate**:  
  * **MaxSurge**: define quantos Pods a mais do que o número desejado (definido em **replicas**) podem existir temporariamente durante a atualização (nesse caso, **1**).  
  * **MaxUnavailable**: define quantos Pods podem ficar indisponíveis ao mesmo tempo durante a atualização.  

Abaixo, a configuração da estratégia de Deployment após a implantação:
<p align="center"><img width="449" height="57" alt="image" src="https://github.com/user-attachments/assets/38549018-8d12-49db-bb76-e4acf452e971" /></p>

Também é possível incluir a quantidade de Pods em **MaxSurge** e **Max Unavailable** com porcentagem de Pods, conforme exemplo abaixo:
<p align="center"><img width="216" height="86" alt="image" src="https://github.com/user-attachments/assets/64e98f9d-4081-465e-a34c-a59a505340b1" /></p>

Abaixo, a nova configuração de estratégia de deployment, agora trabalhando com porcentagem:
<p align="center"><img width="471" height="52" alt="image" src="https://github.com/user-attachments/assets/41bc5a1e-f440-4343-8c9e-3063bb0cc19e" /></p>

**kubectl rollout**  
O **kubectl rollout** é um conjunto de comandos do Kubernetes usado para gerenciar e acompanhar atualizações (rollouts) de recursos que têm estratégia de atualização, principalmente:  
* Deployment;  
* DaemonSet;  
* StatefulSet.  

Ele te ajuda a:  
* Ver o status de uma atualização;  
* Acompanhar o progresso em tempo real;  
* Ver o histórico de versões;  
* Fazer rollback para uma versão anterior.  

O comando **kubectl rollout status** mostra o status do rollout (processo de atualização) de recursos que suportam rollout, não só Deployment.  

Após executar um **kubectl apply** para aplicar um novo deployment, execute **kubectl rollout status deployment nginx-deployment** para verificar o status do rollout atual.

A parte mais importante é o rollout ser finalizado com **successfully rolled out**  
<p align="center"><img width="573" height="51" alt="image" src="https://github.com/user-attachments/assets/0d8fa97a-6366-4ae5-94e1-e299490e4cb5" /></p>

**RecreateStrategy**  
Basicamente, a estratégia de atualização **Recreate** apaga todos os Pods existentes e os cria novamente. Para definir a estratégia de atualização Recreate, defina em Type.
<p align="center"><img width="225" height="42" alt="image" src="https://github.com/user-attachments/assets/fae41eef-2d58-4dfe-be04-31687bd3300d" /></p>

É necessário avaliar qual é a melhor estratégia para cada caso:  

* **RollingUpdate**: melhor para quando os Pods podem ser atualizados aos poucos.  
* **Recreate**: quando é necessária uma troca de versão e todos os Pods precisam ser recriados ao mesmo tempo.  

<h3 align="center"> 03.6. Fazendo rollback e conhecendo o comando rollout. </h3>

**kubectl rollout undo**  
O **kubectl rollout undo** é o comando usado para desfazer (rollback) uma atualização de um recurso do Kubernetes - normalmente um Deployment (mas também funciona com DaemonSet e StatefulSet).  

Em termos simples: ele volta o recurso para uma versão anterior que estava funcionando.  

* **Para executar o rollback**: kubectl rollout undo deployment **nginx-deployment**  
* Utilize **-n** para informar o namespace, se necessário.  

**kubectl rollout history**  
É possível visualizar o histórico de rollout com o comando **kubectl rollout history deployment** e informe o deployment.  
<p align="center"><img width="289" height="72" alt="image" src="https://github.com/user-attachments/assets/72b6218a-ce8d-4ff7-b33d-9c2e3040eb4e" /></p>

<img src="https://img.shields.io/badge/Status-OK-brightgreen?style=for-the-badge"></img>
