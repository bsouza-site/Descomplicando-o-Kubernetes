# Descomplicando o Ingress.
#### 09.1.	O que é o Ingress no Kubernetes?
O Ingress é um recurso do Kubernetes que gerencia o acesso externo aos serviços dentro de um cluster. Ele funciona como uma camada de roteamento HTTP/HTTPS, permitindo a definição de regras para direcionar o tráfego externo para diferentes serviços back-end. O Ingress é implementado através de um controlador de Ingress, que pode ser alimentado por várias soluções, como NGINX, Traefik ou Istio, para citar alguns.

Tecnicamente, o Ingress atua como uma abstração de regras de roteamento de alto nível que são interpretadas e aplicadas pelo controlador de Ingress. Ele permite recursos avançados como balanceamento de carga, SSL/TLS, redirecionamento, reescrita de URL, entre outros.

**Principais componentes e funcionalidades.**  
Abaixo, os principais componentes e funcionalidades do Ingress.

* **Controlador de Ingress**: É a implementação real que satisfaz um recurso Ingress. Ele pode ser implementado através de várias soluções de proxy reverso, como NGINX ou HAProxy.
* **Regras de Roteamento**: Definidas em um objeto YAML, essas regras determinam como as requisições externas devem ser encaminhadas aos serviços internos.
* **Backend Padrão**: Um serviço de fallback para onde as requisições são encaminhadas se nenhuma regra de roteamento for correspondida.
* **Balanceamento de Carga**: Distribuição automática de tráfego entre múltiplos pods de um serviço.
* **Terminação SSL/TLS**: O Ingress permite a configuração de certificados SSL/TLS para a terminação de criptografia no ponto de entrada do cluster.
* **Anexos de Recurso**: Possibilidade de anexar recursos adicionais como ConfigMaps ou Secrets, que podem ser utilizados para configurar comportamentos adicionais como autenticação básica, listas de controle de acesso etc.

**Descontinuação do Ingress NGINX.**  
Para priorizar a segurança do ecossistema, a Kubernetes SIG Network e o Comitê de Resposta a Incidentes de Segurança anunciam a descontinuação do **[Ingress NGINX](https://github.com/kubernetes/ingress-nginx/)**. A manutenção, na medida do possível, continuará até março de 2026. Após essa data, não haverá mais lançamentos, correções de bugs ou atualizações para resolver quaisquer vulnerabilidades de segurança que possam ser descobertas. As instalações existentes do Ingress NGINX continuarão funcionando e os artefatos de instalação permanecerão disponíveis.

Recomendamos migrar para uma das muitas alternativas. Considere **[migrar para a Gateway API](https://gateway-api.sigs.k8s.io/guides/getting-started/)**, a substituta moderna do Ingress. Se precisar continuar usando o Ingress, muitos controladores Ingress alternativos estão **[listados na documentação do Kubernetes](https://kubernetes.io/docs/concepts/services-networking/ingress-controllers/)**. Continue lendo para obter mais informações sobre o histórico e o estado atual do Ingress NGINX, bem como os próximos passos.

#### 09.2.	Configurando o Kind para suportar o Ingress.  
Para configurar o kind para funcionar com o Ingress, crie o arquivo **09_kind-ingress.yml** conforme exemplo abaixo:  
<p align="center"><img width="337" height="277" alt="image" src="https://github.com/user-attachments/assets/648a9372-49d2-4f4c-87c3-e771813c47c9" /></p>

**Agora, crie o cluster com o comando**: sudo kind create cluster --config 09_kind-cluster.yml   
**Obs**: para esse caso, precisei criar o cluster com o sudo.  

#### 09.3.	Instalando o Ingress Nginx Controller.
Verifique o **[Installation Guide](https://kubernetes.github.io/ingress-nginx/deploy/)** para informações de instalação do Nginx Controller.

* **Com isso, execute a instalação do Ingress com o comando**: sudo kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml

Verifique se os serviços foram criados e estão em execução com o comando sudo **kubectl get all**
<p align="center"><img width="982" height="193" alt="image" src="https://github.com/user-attachments/assets/1385cfcc-b98c-46b9-b882-b53c19748bbf" /></p>

#### 09.4.	Deploy da nossa aplicação que iremos usar de exemplo.
Para implantar a aplicação de exemplo, crie o deployment chamado **09_app-deployment.yaml** conforme o exemplo abaixo:
<p align="center"><img width="367" height="402" alt="image" src="https://github.com/user-attachments/assets/aac8a22c-6b40-4da0-add7-168ce7519d62" /></p>

* **env**: variáveis de ambiente que serão passados para a aplicação.  
  * **name**: nome da variável que será passado para a aplicação.  
  * **value**: valor da variável de ambiente.  

* **imagePullPolicy**: em um Deployment do Kubernetes, este parâmetro define quando o kubelet deve baixar (pull) a imagem do container do registry. Os valores possíveis podem ser:
  * **Always**: sempre tentar baixar a imagem do registry, mesmo que já exista localmente no node.  
  * **IfNotPresent**: baixa a imagem somente se ela não existir no node. É o comportamento padrão para imagens com tag diferente de **latest**.  
  * **Never**: nunca faz pull da imagem.  

**Aplique a configuração criada**: sudo kubectl apply -f 09_app-deployment.yaml  

Abaixo, os pods existentes:
<p align="center"><img width="615" height="66" alt="image" src="https://github.com/user-attachments/assets/70408c1a-7e66-4c4f-bc9b-7b299905aadc" /></p>

Agora, é necessário criar o Service... para isso, crie o arquivo **09_app-service.yaml** conforme exemplo abaixo:
<p align="center"><img width="210" height="247" alt="image" src="https://github.com/user-attachments/assets/2e0c359b-079c-4818-93a3-5103b44294fc" /></p>

* **Aplique essa configuração com comando**: sudo kubectl apply -f 09_app-service.yaml

**Para ver o Service e os Endpoints criados, execute**: sudo kubectl get svc ; sudo kubectl get ep
<p align="center"><img width="939" height="132" alt="image" src="https://github.com/user-attachments/assets/42823664-b1e7-4890-bbca-e7a90eef8d48" /></p>

**Configurar o Redis para a aplicação.**  
Crie o arquivo de deployment **09_redis-deployment.yaml** para aplicar a configuração do Redis para a aplicação.
<p align="center"><img width="274" height="450" alt="image" src="https://github.com/user-attachments/assets/095131c8-57e1-41e8-9907-9a7c3d9ff821" /></p>

* **Aplique a configuração criada**: sudo kubectl apply -f 09_redis-deployment.yaml  

Agora, crie o arquivo **09_redis-service.yaml** para ser o Service do Redis.
<p align="center"><img width="195" height="195" alt="image" src="https://github.com/user-attachments/assets/544f18d6-d7f2-4717-81aa-2b89ddff10cc" /></p>

* **Aplique a configuração criada**: sudo kubectl apply -f 09_redis-service.yaml  

Execute o comando **sudo kubectl get all** para verificar se todos os serviços estão configurados corretamente:  
<p align="center"><img width="995" height="353" alt="image" src="https://github.com/user-attachments/assets/4b085a6e-ef36-4342-97f5-6c5905bfe7e2" /></p>

* **Faça o port-forward na porta da aplicação**: sudo kubectl port-forward --address 0.0.0.0 service/giropops-senhas 5000:5000 &  

Após isso, já é possível acessar a aplicação com o IP do servidor na porta **5000**
<p align="center"><img width="869" height="551" alt="image" src="https://github.com/user-attachments/assets/8a7071d6-a08d-4f34-a2f5-f47e043fafd5" /></p>

#### 09.5.	Criando a nossa primeira regra de Ingress.  
Para criar uma regra de Ingress, crie o arquivo **09_ingress-1.yaml** conforme o exemplo:
<p align="center"><img width="402" height="279" alt="image" src="https://github.com/user-attachments/assets/c2275061-b97e-4469-b5be-1650206fabbb" /></p>

Abaixo, uma explicação de todos os parâmetros.  

* **apiVersion**: define a versão da API usada para criar o recurso Ingress.  
* **kind**: Indica que estamos criando um recurso do tipo **Ingress**.  
* **metadata**:  
  * **name**: nome do recurso Ingress dentro do namespace.  
  * **annotations**: essa annotation é específica do NGINX Ingress Controller. Tudo que bater na rota configurada será reescrito para **/** antes de chegar no serviço.  
* **spec**: indica as regras de roteamento.  
  * **rules**: lista de regras de tráfego.  
  * **paths**: lista de caminhos que serão roteados.  
  * **path**: quando alguém acessar **http://IP/giropops-senha**, o tráfego será encaminhado para o back-end definido.  
  * **pathType**: define com o path será interpretado. **Prefix** significa, tudo que começar com **/giropops-senhas**  
* **back-end**: define para onde o tráfego será enviado.  
  * **service**: nome do serviço dentro do cluster.  
  * **port**:  
  * **number**: porta do Service que será utilizada. Isso não é a porta do Pod diretamente, é a porta do Service.  

**Aplique essa configuração**: sudo kubectl apply -f 09_ingress-1.yaml  
**Verifique o Ingress criado**: sudo kubectl get ingress  
<p align="center"><img width="463" height="36" alt="image" src="https://github.com/user-attachments/assets/2fbae9dd-7b11-4ba5-8f4c-ce6d16827a09" /></p>

#### 09.6. Entendendo as alternativas e como resolver.
Neste momento, copie o arquivo **09_ingress-1.yaml** para **09_ingress-2.yaml** e realize as modificações conforme exemplo abaixo:
<p align="center"><img width="290" height="246" alt="image" src="https://github.com/user-attachments/assets/d1befd03-6eff-4a78-9d79-e3c9692dfc6b" /></p>

#### 09.7.	Configurando um Ingress para a nossa App em Flask e usando Redis.
Para este momento, remova todos os Ingress criados com o comando **sudo kubectl delete ingress**  

Copie o arquivo **09_ingress-1.yaml** e crie o arquivo **09_ingress-3.yaml** com o conteúdo abaixo.

<p align="center"><img width="404" height="277" alt="image" src="https://github.com/user-attachments/assets/99e81ff4-51b6-4eda-a8e2-3941066caf45" /></p>

A diferença entre o **09_ingress-1.yaml** para o **09_ingress-3.yaml** é o **path**, que passou de **static** para **/**

* **Aplique essa configuração**: sudo kubectl apply -f 09_ingress-3.yaml

**Para acessar a aplicação de qualquer host, crie o port-forward novamente**: sudo kubectl port-forward --address 0.0.0.0 service/giropops-senhas 5000:5000 &

#### 09.8.	Criando múltiplos Ingresses no mesmo Ingress Controller.
Crie um novo Pod e Service, agora do NGINX. Para isso, execute os passos abaixo:  

* **Crie um pod do Nginx**: sudo kubectl run nginx --image nginx --port 80  
* **Crie o Service**: sudo kubectl expose pod nginx  

Agora, copie o **09_ingress-1.yaml** para o arquivo **09_ingress-4.yaml** conforme o conteúdo abaixo:
<p align="center"><img width="412" height="293" alt="image" src="https://github.com/user-attachments/assets/f989c31a-a14d-4de1-9c41-37dcbec2690d" /></p>

* **Aplique essa configuração**: sudo kubectl apply -f 09_ingress-4.yaml

#### 09.9.	Instalando um cluster EKS para os nossos testes com Ingress.
O EKS (Elastic Kubernetes Service) é o serviço gerenciado de Kubernetes da AWS. Ele permite que você crie, configure e escale clusters Kubernetes sem precisar instalar e manter manualmente o plano de controle (control plane) ou componentes de gerenciamento.

**O que é eksctl?**  
O eksctl é uma ferramenta CLI simples para criar e gerenciar clusters no EKS, o serviço Kubernetes gerenciado da Amazon para EC2. Para ver mais detalhes, acesse a página do projeto: **https://eksctl.io/**

**Download e instalação.**  
Acesse a página **[Installation](https://docs.aws.amazon.com/eks/latest/eksctl/installation.html)** e siga os passos abaixo:  
* **No Linux, crie as variáveis:**  
  * ARCH=amd64
  * PLATFORM=$(uname -s)_$ARCH

* **Baixe o pacote**: curl -sLO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"  
* **Extrair os arquivos**: tar -xzf eksctl_$PLATFORM.tar.gz -C /tmp && rm eksctl_$PLATFORM.tar.gz  
* **Mova os arquivos**: sudo mv /tmp/eksctl /usr/local/bin  

Após estes passos, a ferramenta EKSCTL está instalada no sistema:
<p align="center"><img width="263" height="72" alt="image" src="https://github.com/user-attachments/assets/6b6d6752-73cd-488b-b6a3-824084e43591" /></p>

**O que é AWS CLI?**  
O AWS Command Line Interface (CLI) é uma ferramenta unificada para gerenciar seus serviços da AWS. Com apenas uma ferramenta para baixar e configurar, você pode controlar vários serviços da AWS na linha de comando e automatizá-los por meio de scripts.

A AWS CLI é uma ferramenta unificada que fornece interface consistente para interagir com todas as partes da AWS. Acesse **[Referência de comandos AWS CLI](https://docs.aws.amazon.com/cli/latest/)**.

Acesse a página **[Instruções de instalação e atualização do AWS CLI](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html)**.

**Download e instalação.**
Para instalação no sistema operacional Linux, seguir os passos abaixo.  

* **Baixe o pacote**: curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"  
* **Extrair os arquivos**: unzip awscliv2.zip  
* **Instalar o pacote**: sudo ./aws/install  

Para ver se o pacote foi instalado corretamente, execute: **aws --version**
<p align="center"><img width="582" height="34" alt="image" src="https://github.com/user-attachments/assets/6d91fb3e-c771-4c3e-88e8-6e96a04ff3ed" /></p>

**Adicionando usuário no IAM.**  
Nesta etapa, será necessário criar um grupo para acesso a AWS pelo AWS CLI.  
 **1.**	No IAM, acesse “Grupos de Usuários” e clique em **Criar Grupos de Usuários**.  
 **2.**	Coloque o nome, pode ser **“Administrator AWS Admin for CLI”**.  
 **3.**	Nesta etapa, caso já tenha usuário criado, é possível já adicionar o usuário ao grupo, porém, esta etapa é opcional.  
 **4.**	Em **Associar políticas de permissões**, selecione a permissão **Administrator Access**. Agora, clique em **Criar Grupo**.  

**Configurando o IAM para acesso do AWS CLI.**  
Acesse o IAM e clique em **Criar novo usuário**.  
 **1.**	Crie o nome de usuário.  
 **2.**	Adicione ao grupo **“Administrator AWS Admin for CLI”** que eu já tinha criado.  
 **3.**	Acesse o usuário criado e clique em **Criar chave de acesso.**  
 **4.**	Na opção “Casos de uso”, selecione **Command Line Interface (CLI).**  
<p align="center"><img width="544" height="89" alt="image" src="https://github.com/user-attachments/assets/f5033f16-ff1a-4d45-9d1d-a37ed0885977" /></p>

 **5.**	Na próxima etapa, salve as chaves de acesso ou clique em **Baixar arquivo .csv** para baixar as chaves de acesso.  
 **6.**	Após isso, clique em **Concluir.**  

**Configurando o AWS CLI.**  
Agora, iremos configurar a chave criada no nosso host.  
 **1.**	Digite **aws configure**  
   **a.**	Cole ou digite a **AWS Access Key ID**.  
   **b.**	Cole ou digite a **AWS Secret Access Key**.  
   **c.**	Digite a região que você está usando como default: **us-east-1**  
   **d.**	Em **Default output format**, digite **json**.  

É possível realizar a configuração utilizando um profile especifico:  
* aws configure --profile **bsouza_cli**  

Ao criar a configuração do AWS CLI, é criado arquivos no diretório home do usuário dentro do diretório **.aws.**  
* **config**: contém as configurações.  
* **credentials**: contém as credenciais configuradas com o **aws configure**.  

Realizando a leitura desses arquivos, é possível visualizar quantos perfis existem criados nesse host. Também é possível ver todos os perfis criados neste host, para isso, execute: **aws configure list**  
<p align="center"><img width="587" height="97" alt="image" src="https://github.com/user-attachments/assets/0320cfc3-9692-4586-8b5c-2c5bc6bb56fa" /></p>

**Para instalação do cluster EKS, execute**: eksctl create cluster --name=eks-cluster --version=1.30 --region=us-east-1 --nodegroup-name=eks-cluster-nodegroup --node-type=t3.medium --nodes=2 --nodes-min=1 --nodes-max=3 --managed  

#### 09.10.	Entendendo os Contexts do Kubernetes para gerenciar vários clusters.
No Kubernetes, **Context** é o que define para qual cluster você está apontando e com qual usuário o kubectl vai se conectar.  

Executando **kubectl get nodes**, é possível visualizar os nodes criados na AWS com o eksctl.
<p align="center"><img width="622" height="50" alt="image" src="https://github.com/user-attachments/assets/a7b7274d-05e5-469a-bf13-76652bff01cc" /></p>

Abaixo, mais comandos para trabalhar com contextos no Kubernetes.  
* **Verificar o contexto atual**: kubectl config current-context  
* **Verificar os contextos existentes**: kubectl config get-contexts  
* **Alterar para um contexto existente**: kubectl config use-context  

#### 09.11. Instalando o Ingress Nginx Controller no EKS.
**Para instalar o Ingress no EKS da AWS, execute**: kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.2/deploy/static/provider/aws/deploy.yaml  

**Obs**: lembrando que o contexto padrão deve ser o cluster EKS da AWS, se o contexto padrão estiver o Kind, esse Ingress vai ser instalado no Kind, e não no EKS.  

Para ver todos os serviços criados no cluster EKS da AWS, execute **kubectl get all -n ingress-nginx**
<p align="center"><img width="730" height="338" alt="image" src="https://github.com/user-attachments/assets/6d22b2ca-29cc-40c3-b0cc-f220cc4a8b2f" /></p>

#### 09.12.	Conhecendo o ingressClassName e configurando um novo Ingress.

#### 09.13.	Configurando um domínio válido para o nosso Ingress no EKS.







